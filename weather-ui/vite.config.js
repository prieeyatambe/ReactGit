import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
export default defineConfig({
    plugins: [react()],
});
// This is a Vite configuration file that sets up a React project with the necessary plugins.
// It imports the `defineConfig` function from Vite and the React plugin, then exports a configuration object that includes the React plugin.
